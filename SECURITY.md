# 安全说明

## 适用范围

当前为教学演示 MVP，仅维护 `main` 上的版本，没有生产安全支持或响应时限承诺。

目前没有完整的登录鉴权、角色授权、租户隔离或调用限流。客户端传入的教师/学生 ID 不能代替身份认证；包括 `/api/demo/reset` 在内的接口不能直接暴露到公网。CORS 配置也不能代替访问控制。

## 安全部署

- 使用演示数据，在本机或受限网络中运行。接入真实学生数据前必须补齐鉴权、授权、数据隔离、备份及适当的数据处理流程。
- Compose 的 PostgreSQL、MinIO 密码必须由部署者设置；分别使用 `openssl rand -hex 24` 生成独立值，不要复用密码。
- 所有映射端口默认只绑定 `127.0.0.1`；管理访问使用 SSH 隧道。前端反向代理也能访问后端 API，因此开放前端前仍需完成权限控制。
- 真实 AI / OCR / ASR 调用会把相关文本、图片或音频发送到配置的服务。使用最小权限凭据、预算限制和有权处理的材料；不要在测试中使用真实学生个人数据。
- `.env`、环境变体、私钥、数据库和日志不应提交。`.gitignore` 不能移除已经被 Git 跟踪的文件，也不能擦除历史。
- `VITE_*` 环境变量是前端公开配置，不得保存服务端密钥。不要公开输出 `docker compose config` 的完整结果，其中可能包含插值后的密码；检查配置使用 `--quiet`。
- CI 中的凭据扫描只能检测已知模式，不能证明不存在泄露。发布前仍需人工检查新增文件与 Git 历史。

## 报告安全问题

若仓库 Security 页面提供 **Report a vulnerability**，请使用该私密报告入口。若入口不可用，可提交不含利用步骤、凭据或个人数据的 Issue，仅请求维护者建立私密沟通渠道；私密渠道建立前不要上传敏感细节。

报告可包含受影响版本、问题类型、影响范围和已脱敏的复现说明。不要发送真实 token、密码、学生信息或完整环境文件。

## 发现凭据泄露时

先在对应服务商处撤销或轮换凭据，再更新部署配置并检查调用/访问记录。仅删除文件或把仓库改回私有不足以使已泄露凭据失效。需要清理 Git 历史时，应协调协作者并备份；已被复制的内容无法保证收回。
